{   "homepage": "http://dfortune014.github.io/ChatBot",
    "name": "mern-chatbot",
    "version": "1.0.0",
    "scripts": {
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build",
        "dev": "npx concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix Backend\"",
        "start": "npx concurrently \"npm start --prefix frontend\" \"npm start --prefix Backend\"",
        "install-all": "npm install --prefix frontend && npm install --prefix Backend"
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    },
    "dependencies": {
        "gh-pages": "^6.1.1"
    }
}
